# 🏭 Qaori Factory Playground

This is a sandbox repository for training AI agents.

## Scenario: Broken Calculator

The `src/calculator.py` has a bug in division.
The `tests/test_calculator.py` fails because it expects `ValueError` but gets `ZeroDivisionError`.

**Goal**: Fix the code and the test.
